### select a python version

```shell
poetry env use 3.10
```

### start the venv

```shell
poetry shell
```

### install packages

```shell
poetry lock
poetry install
```

### remove venv

```shell
poetry env list
poetry env remove <venv-name>
```
